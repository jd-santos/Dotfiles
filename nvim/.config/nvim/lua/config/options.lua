-- Options that override LazyVim defaults
-- LazyVim sets most options by default, only override what differs

vim.opt.wrap = false -- Disable line wrapping (LazyVim defaults to true)

-- Optional: Keep explicit UTF-8 settings (usually not needed but harmless)
vim.opt.encoding = "utf-8"
vim.opt.fileencoding = "utf-8"
