---
description: Review the current branch, commit ready work, and push after confirmation
argument-hint: "[extra instructions]"
---
# Ship

Load and run the `ship` skill.

Extra user instructions, if any:

$ARGUMENTS
